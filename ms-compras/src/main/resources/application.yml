spring:
  main:
    allow-bean-definition-overriding: true
  application:
    name: ms-compra
  profiles:
    active: development
  config:
    import: optional:configserver:http://root:123456@localhost:6065
